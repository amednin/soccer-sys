{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pimp creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('pimp') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
